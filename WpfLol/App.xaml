<Application x:Class="WpfLol.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfLol"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        <Style TargetType="{x:Type Border}" x:Key="GoldLine">
            <Setter Property="Background" Value="#00070E" />
            <Setter Property="BorderBrush" Value="#34291E" />
            <Setter Property="BorderThickness" Value="1 1 1 1" />
            <Setter Property="Margin" Value="10 0 0 0" />
        </Style>
        <Style TargetType="{x:Type Image}" x:Key="Emblem">
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="RenderOptions.BitmapScalingMode" Value="Fant" />
            <Setter Property="Source" Value="Images/logo.png" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Opacity" Value="0.3" />
        </Style>
        <Style TargetType="{x:Type ToggleButton}">
             <Setter Property="Height" Value="38" />
             <Setter Property="Width" Value="165" />
             <Setter Property="Foreground" Value="#FFFFFF" />
             <Setter Property="Background" Value="Transparent" />
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="{x:Type ToggleButton}">
                         <Grid Background="{TemplateBinding Background}">
                             <Border Style="{StaticResource GoldLine}"></Border>
                             <Image Style="{StaticResource Emblem}"></Image>
                             <TextBlock Text="{TemplateBinding Content}"></TextBlock>
                         </Grid>
                     </ControlTemplate>
                 </Setter.Value>
                 
             </Setter>
         </Style>
            </ResourceDictionary>
    </Application.Resources>
</Application>
